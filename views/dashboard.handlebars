    <div class="">
        <a href="/dashboard/create/"><button class="btn btn-success mt-2">create post</button></a>
        
        <h2>Your posts</h2><hr>
{{#if posts.length}}
    </div>

    {{#each posts as |post|}}
        {{> post-info post}}
        
            <a href="/dashboard/edit/{{post.id}}"><button class="btn btn-success">edit post</button></a>
        </div>
    {{/each}}

{{else}}
<h6 class="">you do not currently have any posts</h6>

{{/if}}


