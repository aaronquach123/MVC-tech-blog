<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Crazy Tech Tips</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-wEmeIV1mKuiNpC+IOBjI7aAzPcEZeedi5yW5f2yOq55WWLwNGmvvx4Um1vskeMj0" crossorigin="anonymous">
  <link rel="stylesheet" href="/stylesheets/style.css">
  <script defer src="https://use.fontawesome.com/releases/v5.3.1/js/all.js"></script>
</head>

  <div class="container">
    <header class="d-block bg-dark text-white">
        <h1 class="ml-3">Crazy Tech Tips</h1>
        <nav class="nav justify-content-end m-1">
            {{#if loggedIn}}
            <a href="/"><button class="btn btn-primary m-1">Home</button></a>
            <a href="/dashboard"><button class="btn btn-primary m-1">Dashboard</button></a>
            <a href="/dashboard/create/"><button class="btn btn-primary m-1">Create post</button></a>
            <button id="logout" class="btn btn-danger m-1">Logout</button>
            {{else}}
            <a href="/"><button class="btn btn-primary m-1">Home</button></a>
            <a href="/login"><button class="btn btn-success m-1"></button>Login</a>
            {{/if}}
        </nav>
    </header>

    <main>
            {{{ body }}}
    </main>

    <footer class="footer bg-dark d-flex mt-4">
        <div class="mt-4">
            <p>
            <a href="/"><button class="btn btn-primary m-1">Home</button></a>{{#if loggedIn}} | 
            <a href="/dashboard/"><button class="btn btn-primary m-1">Dashboard</button></a> | 
            <a href="/dashboard/create/"><button class="btn btn-success m-1">Create new post</button></a>
            {{else}}
             | <a href="/login/"><button class="btn btn-success m-1">Login</button></a>
            {{/if}}
            </p>
        </div>
      <div>
  </div>
    </footer>
  </div>

{{#if loggedIn}}
<script src="/javascript/logout.js"></script>
{{/if}}
</body>

</html>